package com.aygxy.service;

import com.aygxy.base.Result;
import com.aygxy.jpa.entity.Department;
import org.springframework.data.domain.Pageable;

/**
 * @Description: 业务实现层接口
 * @Author: xmf
 * @Date: 2019/4/11-18:37
 */
public interface DepartmentService {

    ////添加部门
    //Result add(Department department);
    ////删除部门
    //Result delete(String uid);
    ////编辑信息
    //Result update(String uid,Department department);
    ////分页查询
    //Result findAll(Pageable pageable);
    ////动态查询
    //Result dynamicQuery(Pageable pageable,Department department);
}
